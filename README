Welcome to README of MeetiX OS.

# restyle of repository.
# now all dependecies (except qemu, virtualbox, mpc, mpfr and gmp) for meetix toolchains are auto downloaded and local installed by the build script on toolchain directory.
# go to toolchain directory with a terminal and type
> bash build.sh # this take several minutes, all dependencies will be downloaded and compiled, libs will be installed on /opt/MeetiXOSProject/

# if previous command finish successful type
> bash build.sh eva    # this compile the kernel
> bash build.sh user   # this compile the applications of MeetiX OS
> bash build.sh repack # this create iso of MeetiX and run qemu.
